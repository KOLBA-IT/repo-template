#!/bin/sh
# Локальная защита от случайного push в main
branch="$(git rev-parse --abbrev-ref HEAD)"
if [ "$branch" = "main" ]; then
  echo "❌ Нельзя пушить прямо в main! Создайте ветку и откройте PR."
  exit 1
fi
